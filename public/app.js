const brands = ['civicaNews','The Telegram','StateLine','InsideStory','Caprica Now','The Sentinel','WorldDesk','GlobalWire','Dispatch','Meridian','WorldView','ForeignAffairs','regions.news','Caille Times','Montiablo Times','CapitalNow','The Chronicle','LocalGovernment'];
const $ = s => document.querySelector(s);
async function api(path, opts={}){const r=await fetch(path,{headers:{'content-type':'application/json'},...opts}); if(!r.ok) throw new Error((await r.json().catch(()=>({error:r.statusText}))).error); return r.json()}
function nav(){return `<header class="top"><div class="wrap nav"><a class="brand" href="/">Caprica Media Group</a><nav class="navlinks"><a href="/brands.html">Brands</a><a href="/standards.html">Standards</a><a href="/admin.html">Admin</a></nav></div></header>`}
function foot(){return `<footer class="footer"><div class="wrap"><b>civicaGroup</b><p>The views and opinions expressed by individual publications, contributors, commentators, or guests do not necessarily reflect those of Caprica Media Group, Civica Holdings, or affiliated organisations. Editorial positions remain the responsibility of each publication.</p></div></footer>`}
async function renderHome(){document.body.insertAdjacentHTML('afterbegin',nav()); $('#foot').innerHTML=foot(); $('#brandgrid').innerHTML=brands.map(b=>`<a class="brandpill" href="/brand.html?brand=${encodeURIComponent(b)}">${b}</a>`).join(''); const posts=await api('/api/posts').catch(()=>[]); $('#latest').innerHTML=posts.length?posts.slice(0,6).map((p,i)=>`<a class="card ${i==0?'lead':''}" href="/article.html?slug=${p.slug}"><span class="tag">${p.brand} / ${p.category}</span><h3>${p.title}</h3><p>${p.dek}</p><span class="meta">${new Date(p.published_at||p.created_at).toLocaleDateString()}</span></a>`).join(''):'<div class="card"><h3>No published stories yet</h3><p>Log in as an admin to create the first article.</p></div>'}
async function renderBrand(){document.body.insertAdjacentHTML('afterbegin',nav()); $('#foot').innerHTML=foot(); const brand=new URLSearchParams(location.search).get('brand')||'civicaNews'; $('#brandName').textContent=brand; const posts=await api('/api/posts?brand='+encodeURIComponent(brand)).catch(()=>[]); $('#posts').innerHTML=posts.map(p=>`<a class="card" href="/article.html?slug=${p.slug}"><span class="tag">${p.category}</span><h3>${p.title}</h3><p>${p.dek}</p></a>`).join('')||'<div class="card">No published stories for this brand yet.</div>'}
async function renderArticle(){document.body.insertAdjacentHTML('afterbegin',nav()); $('#foot').innerHTML=foot(); const slug=new URLSearchParams(location.search).get('slug'); const p=await api('/api/posts/'+slug); $('#article').innerHTML=`<span class="tag">${p.brand} / ${p.category}</span><h1>${p.title}</h1><p class="meta">By ${p.author_email} · ${new Date(p.published_at||p.created_at).toLocaleString()}</p><p style="font-size:22px;color:var(--muted)">${p.dek}</p><div class="body">${p.body}</div>`}
async function renderAdmin(){document.body.insertAdjacentHTML('afterbegin',nav()); $('#foot').innerHTML=foot(); brands.forEach(b=>$('#brand').insertAdjacentHTML('beforeend',`<option>${b}</option>`)); async function refresh(){const me=await api('/api/auth/me').catch(()=>({user:null})); $('#login').classList.toggle('hidden',!!me.user); $('#dash').classList.toggle('hidden',!me.user); if(me.user){$('#who').textContent=`${me.user.email} / ${me.user.role} / ${me.user.brand}`; const posts=await api('/api/admin/posts').catch(()=>[]); $('#adminPosts').innerHTML=posts.map(p=>`<div class="card"><span class="tag">${p.status} / ${p.brand}</span><h3>${p.title}</h3><p>${p.dek}</p></div>`).join('')}} await refresh(); $('#loginForm').onsubmit=async e=>{e.preventDefault(); await api('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))}); refresh()}; $('#postForm').onsubmit=async e=>{e.preventDefault(); await api('/api/admin/posts',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))}); e.target.reset(); refresh()}; $('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'}); refresh()}}
